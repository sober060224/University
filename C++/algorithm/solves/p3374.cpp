#include <iostream>
#include <vector>
#include <algorithm>
#define int long long
using namespace std;

class Fenwick
{
public:
    vector<int> tree;

    Fenwick(int n)
    {
        tree.resize(n + 1, 0);
    }

    
};

void solve()
{
}

signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);
}